
import "./std/prelude.mvm";

let msg1: string = "Hey123\n";

type Ptr(u64);

fun main(:) {
    let msg2: string = "Hey123\n";

    msg2 dup(string:string,string)

    len(string:u64) debug(u64:)

    write(string:u64) drop(u64:)

    5u64 to(u64:Ptr) to(Ptr:u64) drop(u64:)

    msg1 write(string:u64) drop(u64:)
    msg2 write(string:u64) drop(u64:)
}

fun{inline} to(u64:Ptr) {
    wrap(Ptr)
}

fun{inline} to(Ptr:u64) {
    unwrap
}
